import { ArrowRight, Check, ChevronLeft, Clock3, Leaf, Minus, Plus, Search, ShieldCheck, Sparkles, Trash2, Utensils, X } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import { Link, useLocation, useParams } from 'wouter';
import { DishArt, DishCard } from '@/components/dish-card';
import { useStorefront } from '@/hooks/use-storefront';
import { categories, dishes, findDish, formatPrice, type Category } from '@/lib/catalog';

export function HomePage() {
  const { addToCart } = useStorefront();
  const feature = dishes[0];
  return <div>
    <section className="relative overflow-hidden bg-secondary/60">
      <div className="mx-auto grid max-w-[1240px] items-center gap-10 px-5 py-14 sm:px-8 sm:py-20 lg:grid-cols-[1.02fr_.98fr] lg:gap-20 lg:py-24">
        <div className="animate-rise">
          <div className="mb-7 flex items-center gap-3 text-[11px] font-bold uppercase tracking-[.2em] text-accent"><span className="h-px w-8 bg-accent" /> Nutrition, without the noise</div>
          <h1 data-testid="heading-home" className="max-w-2xl font-display text-[clamp(3.2rem,8vw,6.8rem)] font-semibold leading-[.91] tracking-[-.055em] text-primary">A better<br /><em className="font-normal text-accent">everyday</em><br />starts here.</h1>
          <p className="mt-8 max-w-md text-base leading-7 text-muted-foreground sm:text-lg">Dietitian-designed meals for real life. Warm, balanced, and delivered from our kitchen to your door in Noida.</p>
          <div className="mt-9 flex flex-wrap items-center gap-3"><Link href="/menu" data-testid="link-hero-menu" className="group inline-flex h-13 items-center gap-3 rounded-full bg-primary px-6 text-sm font-bold text-primary-foreground transition-all hover:bg-accent hover:text-accent-foreground">Browse today’s menu <ArrowRight size={17} className="transition-transform group-hover:translate-x-1" /></Link><Link href="/plans" data-testid="link-hero-plans" className="inline-flex h-13 items-center rounded-full border border-primary/20 px-6 text-sm font-bold text-primary transition-colors hover:bg-card">Find your rhythm</Link></div>
          <div className="mt-12 flex gap-8 border-t border-primary/15 pt-5"><div><p className="font-display text-2xl text-primary">24g+</p><p className="text-[10px] font-bold uppercase tracking-[.14em] text-muted-foreground">protein in most mains</p></div><div><p className="font-display text-2xl text-primary">1 kitchen</p><p className="text-[10px] font-bold uppercase tracking-[.14em] text-muted-foreground">Noida · cooked daily</p></div></div>
        </div>
        <div className="relative mx-auto w-full max-w-[560px] animate-rise animate-float lg:mt-4">
          <div className="absolute -right-2 top-8 z-10 max-w-[170px] rounded-2xl border border-border bg-card/90 p-4 shadow-[var(--shadow-soft)] backdrop-blur"><div className="mb-2 flex items-center gap-2 text-accent"><ShieldCheck size={16} /><span className="text-[10px] font-bold uppercase tracking-[.14em]">RD reviewed</span></div><p className="text-xs leading-4 text-primary">Macros estimated from ingredients, never guessed.</p></div>
          <DishArt dish={feature} className="aspect-square rounded-[2.5rem] border border-primary/10 shadow-[var(--shadow-lift)]" />
          <div className="absolute -bottom-5 left-5 right-5 flex items-center justify-between rounded-2xl border border-border bg-card px-5 py-4 shadow-[var(--shadow-soft)]"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-muted-foreground">Tonight’s good idea</p><p className="mt-1 font-display text-xl font-semibold text-primary">{feature.name}</p></div><button type="button" data-testid="button-add-hero-dish" onClick={() => addToCart(feature)} className="flex h-11 w-11 items-center justify-center rounded-full bg-accent text-accent-foreground transition-transform hover:scale-105"><Plus size={19} /></button></div>
        </div>
      </div>
    </section>
    <section className="mx-auto max-w-[1240px] px-5 py-20 sm:px-8 sm:py-28">
      <div className="mb-9 flex items-end justify-between gap-4"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">The kitchen is open</p><h2 className="mt-3 font-display text-4xl font-semibold leading-none text-primary sm:text-5xl">Made for this moment.</h2></div><Link href="/menu" data-testid="link-home-view-menu" className="hidden items-center gap-2 text-sm font-bold text-primary sm:flex">See all dishes <ArrowRight size={16} /></Link></div>
      <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{dishes.slice(0, 4).map((dish, index) => <DishCard key={dish.id} dish={dish} index={index} />)}</div>
      <Link href="/menu" data-testid="link-home-view-menu-mobile" className="mt-7 flex items-center justify-center gap-2 text-sm font-bold text-primary sm:hidden">See all dishes <ArrowRight size={16} /></Link>
    </section>
    <section className="bg-primary text-primary-foreground"><div className="mx-auto grid max-w-[1240px] gap-10 px-5 py-16 sm:px-8 sm:py-20 md:grid-cols-[.85fr_1.15fr] md:items-center"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">The Tanmatra difference</p><h2 className="mt-4 max-w-md font-display text-4xl leading-[.98] sm:text-5xl">You bring the goal. We bring the next meal.</h2></div><div className="grid gap-7 sm:grid-cols-3">{[['01', 'Designed by dietitians', 'Every recipe starts with a reason, not a trend.'], ['02', 'Cooked like food', 'Whole spices, cold-pressed oils, zero packet shortcuts.'], ['03', 'Delivered with ease', 'Choose a day, choose a meal, get on with your life.']].map(([n, title, body]) => <div key={n} className="border-l border-primary-foreground/20 pl-5"><span className="font-data text-xs text-accent">{n}</span><h3 className="mt-4 font-display text-xl">{title}</h3><p className="mt-2 text-sm leading-5 text-primary-foreground/65">{body}</p></div>)}</div></div></section>
    <section className="mx-auto max-w-[1240px] px-5 py-20 sm:px-8 sm:py-28"><div className="grid items-center gap-12 lg:grid-cols-[1fr_1.1fr]"><div className="rounded-[2rem] bg-secondary p-8 sm:p-12"><div className="mb-8 flex h-12 w-12 items-center justify-center rounded-full bg-accent text-accent-foreground"><Sparkles size={20} /></div><p className="text-[11px] font-bold uppercase tracking-[.18em] text-accent">A gentler way to commit</p><h2 className="mt-4 max-w-md font-display text-4xl font-semibold leading-[1] text-primary sm:text-5xl">Make lunch the easiest healthy decision.</h2><p className="mt-5 max-w-sm text-sm leading-6 text-muted-foreground">Plans that flex around your week, from three trial days to a whole month of meals you look forward to.</p><Link href="/plans" data-testid="link-home-plan-cta" className="mt-8 inline-flex items-center gap-2 text-sm font-bold text-primary">Explore meal plans <ArrowRight size={16} /></Link></div><div className="grid gap-8 sm:grid-cols-2">{[['A', 'Steady', 'Balanced meals for the pace of a normal week.'], ['B', 'Desk Fuel', 'High-protein lunches for busy workdays.'], ['C', 'Protein Build', 'A little more structure for training days.'], ['D', 'Start small', 'Try three days before you make it a habit.']].map(([letter, title, body]) => <div key={letter} className="flex gap-4 border-b border-border pb-7"><span className="font-display text-3xl text-accent">{letter}</span><div><h3 className="font-display text-2xl font-semibold text-primary">{title}</h3><p className="mt-2 text-sm leading-5 text-muted-foreground">{body}</p></div></div>)}</div></div></section>
  </div>;
}

export function MenuPage() {
  const [location] = useLocation();
  const [query, setQuery] = useState('');
  const [category, setCategory] = useState<'All' | Category>('All');
  const [vegOnly, setVegOnly] = useState(false);
  const favoriteQuery = new URLSearchParams(location.split('?')[1] ?? '').get('favorites') === 'true';
  const [favoritesOnly, setFavoritesOnly] = useState(favoriteQuery);
  const [loading, setLoading] = useState(true);
  const { favorites } = useStorefront();
  useEffect(() => { const timer = window.setTimeout(() => setLoading(false), 360); return () => window.clearTimeout(timer); }, []);
  useEffect(() => { setFavoritesOnly(favoriteQuery); }, [favoriteQuery]);
  const filtered = useMemo(() => dishes.filter((dish) => (category === 'All' || dish.category === category) && (!vegOnly || dish.isVeg) && (!favoritesOnly || favorites.includes(dish.slug)) && (!query || `${dish.name} ${dish.description} ${dish.kitchen}`.toLowerCase().includes(query.toLowerCase()))), [category, favorites, favoritesOnly, query, vegOnly]);
  return <div className="mx-auto max-w-[1240px] px-5 py-12 sm:px-8 sm:py-16">
    <div className="max-w-2xl animate-rise"><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">The Tanmatra menu</p><h1 data-testid="heading-menu" className="mt-3 font-display text-5xl font-semibold leading-[.95] tracking-[-.04em] text-primary sm:text-7xl">Good food,<br /><em className="font-normal text-accent">well considered.</em></h1><p className="mt-6 text-base leading-7 text-muted-foreground">A menu of fresh, balanced meals built for how you actually eat. Start with a craving, finish with a meal that makes sense.</p></div>
    <div className="mt-11 flex flex-col gap-4 border-y border-border py-5"><div className="relative max-w-xl"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground" size={18} /><input type="search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search chicken, khichdi, something fresh..." data-testid="input-menu-search" className="h-12 w-full rounded-xl border border-border bg-card pl-11 pr-10 text-sm text-primary placeholder:text-muted-foreground focus:border-primary focus:ring-2 focus:ring-accent/30" />{query && <button type="button" aria-label="Clear search" data-testid="button-clear-search" onClick={() => setQuery('')} className="absolute right-3 top-1/2 -translate-y-1/2 rounded-full p-1 text-muted-foreground hover:text-primary"><X size={16} /></button>}</div><div className="flex gap-2 overflow-x-auto pb-1 no-scrollbar">{categories.map((item) => <button type="button" key={item} data-testid={`button-category-${item.toLowerCase().replace(/[^a-z]+/g, '-')}`} onClick={() => setCategory(item)} className={`shrink-0 rounded-full px-4 py-2 text-xs font-bold transition-colors ${category === item ? 'bg-primary text-primary-foreground' : 'bg-secondary text-muted-foreground hover:bg-secondary/70 hover:text-primary'}`}>{item}</button>)}<button type="button" data-testid="button-filter-vegetarian" aria-pressed={vegOnly} onClick={() => setVegOnly(!vegOnly)} className={`shrink-0 rounded-full border px-4 py-2 text-xs font-bold ${vegOnly ? 'border-accent bg-accent/15 text-primary' : 'border-border text-muted-foreground hover:text-primary'}`}><Leaf size={13} className="mr-1 inline" /> Vegetarian</button><button type="button" data-testid="button-filter-favorites" aria-pressed={favoritesOnly} onClick={() => setFavoritesOnly(!favoritesOnly)} className={`shrink-0 rounded-full border px-4 py-2 text-xs font-bold ${favoritesOnly ? 'border-accent bg-accent/15 text-primary' : 'border-border text-muted-foreground hover:text-primary'}`}>Saved dishes</button></div></div>
    <div className="mt-9 flex items-center justify-between"><p data-testid="status-menu-count" className="text-sm text-muted-foreground">{loading ? 'Setting the table...' : `${filtered.length} dishes to choose from`}</p><span className="hidden items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-muted-foreground sm:flex"><Clock3 size={14} /> Made fresh daily</span></div>
    {loading ? <div className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{[1, 2, 3, 4, 5, 6].map((item) => <div key={item} data-testid={`skeleton-dish-${item}`} className="h-[430px] animate-pulse rounded-2xl bg-secondary" />)}</div> : filtered.length > 0 ? <div className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((dish, index) => <DishCard key={dish.id} dish={dish} index={index} />)}</div> : <div data-testid="empty-menu-state" className="mt-5 flex min-h-[340px] flex-col items-center justify-center rounded-2xl border border-dashed border-border bg-card px-5 text-center"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-secondary text-primary"><Search size={22} /></div><h2 className="mt-5 font-display text-2xl font-semibold text-primary">{favoritesOnly ? 'No saved dishes yet' : 'Nothing by that name'}</h2><p className="mt-2 max-w-sm text-sm text-muted-foreground">{favoritesOnly ? 'Tap the heart on any dish to keep it close.' : 'Try a broader search, or let us surprise you from the full menu.'}</p><button type="button" data-testid="button-reset-menu" onClick={() => { setQuery(''); setCategory('All'); setVegOnly(false); setFavoritesOnly(false); }} className="mt-6 rounded-full bg-primary px-5 py-3 text-xs font-bold text-primary-foreground">Show full menu</button></div>}
  </div>;
}

export function DishPage() {
  const { slug = '' } = useParams<{ slug: string }>();
  const dish = findDish(slug);
  const { addToCart } = useStorefront();
  if (!dish) return <div className="mx-auto max-w-xl px-5 py-28 text-center"><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">404 / dish not found</p><h1 className="mt-4 font-display text-5xl text-primary">That plate has left the kitchen.</h1><Link href="/menu" data-testid="link-back-menu-missing-dish" className="mt-7 inline-flex items-center gap-2 rounded-full bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Back to menu <ArrowRight size={16} /></Link></div>;
  return <div className="mx-auto max-w-[1240px] px-5 py-10 sm:px-8 sm:py-16"><Link href="/menu" data-testid="link-back-menu" className="mb-9 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.16em] text-muted-foreground hover:text-primary"><ChevronLeft size={15} /> Back to menu</Link><div className="grid gap-10 lg:grid-cols-[1.03fr_.97fr] lg:items-center lg:gap-20"><DishArt dish={dish} className="aspect-square rounded-[2rem] sm:rounded-[2.5rem]" /><div className="animate-rise"><div className="flex items-center gap-3 text-[11px] font-bold uppercase tracking-[.18em] text-accent"><span>{dish.category}</span><span className="h-1 w-1 rounded-full bg-accent" /><span>{dish.kitchen} kitchen</span></div><h1 data-testid="heading-dish-detail" className="mt-5 font-display text-5xl font-semibold leading-[.94] tracking-[-.04em] text-primary sm:text-7xl">{dish.name}</h1><p className="mt-6 max-w-lg text-base leading-7 text-muted-foreground">{dish.description}</p><div className="mt-8 grid grid-cols-3 border-y border-border py-5"><Macro value={`${dish.protein}g`} label="protein" /><Macro value={`${dish.calories}`} label="calories" /><Macro value={`${dish.fiber}g`} label="fiber" /></div><div className="mt-8 flex items-center justify-between gap-4"><div><p data-testid="text-dish-detail-price" className="font-data text-2xl font-bold text-primary">{formatPrice(dish.price)}</p><p className="mt-1 text-xs text-muted-foreground">one generous serving</p></div><button type="button" data-testid="button-add-dish-detail" onClick={() => addToCart(dish)} className="flex h-13 flex-1 items-center justify-center gap-2 rounded-full bg-primary px-6 text-sm font-bold text-primary-foreground transition-colors hover:bg-accent hover:text-accent-foreground sm:max-w-[250px]">Add to bag <Plus size={17} /></button></div><p className="mt-5 flex items-center gap-2 text-xs text-muted-foreground"><ShieldCheck size={15} className="text-sage-deep" /> Ingredient-derived estimates · prepared fresh today</p></div></div><div className="mt-20 grid gap-10 border-t border-border pt-12 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-[11px] font-bold uppercase tracking-[.18em] text-accent">What’s in the box</p><h2 className="mt-3 font-display text-4xl text-primary">Simple, familiar, considered.</h2></div><div className="grid gap-4 sm:grid-cols-2">{dish.ingredients.map((ingredient) => <div key={ingredient} className="flex items-center gap-3 border-b border-border pb-4 text-sm text-muted-foreground"><Check size={16} className="shrink-0 text-accent" />{ingredient}</div>)}</div></div></div>;
}

function Macro({ value, label }: { value: string; label: string }) { return <div><p data-testid={`macro-${label}`} className="font-display text-2xl font-semibold text-primary">{value}</p><p className="mt-1 text-[10px] font-bold uppercase tracking-[.14em] text-muted-foreground">{label}</p></div>; }

const plans = [
  { id: 'plan-steady', name: 'Steady', eyebrow: 'For a balanced week', price: 2499, meals: '10 meals / month', detail: 'A practical rotation of balanced lunches and dinners that keeps good intentions easy.', tint: 'bg-secondary' },
  { id: 'plan-desk-fuel', name: 'Desk Fuel', eyebrow: 'For full calendars', price: 2799, meals: '10 high-protein meals / month', detail: 'Protein-forward meals for days when your brain has three tabs open and lunch is one of them.', tint: 'bg-[#e8d5bd]' },
  { id: 'plan-protein-build', name: 'Protein Build', eyebrow: 'For training days', price: 3199, meals: '12 protein-led meals / month', detail: 'More protein, more structure, still food you genuinely want to eat.', tint: 'bg-[#d9e0ce]' },
];

export function PlansPage() {
  const { addPlan } = useStorefront();
  const [, setLocation] = useLocation();
  return <div className="mx-auto max-w-[1240px] px-5 py-12 sm:px-8 sm:py-16"><div className="max-w-3xl animate-rise"><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">Plans that meet you there</p><h1 data-testid="heading-plans" className="mt-3 font-display text-5xl font-semibold leading-[.93] tracking-[-.04em] text-primary sm:text-7xl">A little structure.<br /><em className="font-normal text-accent">A lot less thinking.</em></h1><p className="mt-6 max-w-xl text-base leading-7 text-muted-foreground">Start with a rhythm that feels like yours. Pause, skip or change your plan whenever your week changes.</p></div><div className="mt-14 grid gap-5 lg:grid-cols-3">{plans.map((plan, index) => <article key={plan.id} data-testid={`card-plan-${plan.id}`} className={`relative flex flex-col overflow-hidden rounded-2xl border border-border ${plan.tint} p-7 transition-transform duration-300 hover:-translate-y-1 hover:shadow-[var(--shadow-lift)] ${index === 1 ? 'lg:-translate-y-4' : ''}`}><span className="text-[10px] font-bold uppercase tracking-[.18em] text-accent">{plan.eyebrow}</span><h2 className="mt-5 font-display text-4xl font-semibold text-primary">{plan.name}</h2><p className="mt-3 min-h-[72px] text-sm leading-6 text-muted-foreground">{plan.detail}</p><div className="mt-8 border-t border-primary/15 pt-5"><p className="font-data text-2xl font-bold text-primary">{formatPrice(plan.price)}<span className="font-sans text-xs font-normal text-muted-foreground"> / month</span></p><p className="mt-1 text-xs text-muted-foreground">{plan.meals}</p></div><button type="button" data-testid={`button-choose-plan-${plan.id}`} onClick={() => { addPlan(plan.id, plan.name, plan.price); setLocation('/cart'); }} className="mt-7 flex h-12 items-center justify-center gap-2 rounded-full bg-primary text-sm font-bold text-primary-foreground transition-colors hover:bg-accent hover:text-accent-foreground">Choose {plan.name} <ArrowRight size={16} /></button></article>)}</div><section className="mt-24 grid gap-8 border-t border-border pt-12 sm:grid-cols-3">{[['Flexible by design', 'Pause or skip a delivery whenever life gets loud.'], ['Guided by care', 'Every meal is built with nutrition, flavour and your day in mind.'], ['Fresh, not frozen', 'Your meals are cooked in our Noida kitchen and delivered fresh.']].map(([title, body]) => <div key={title} className="flex gap-4"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-secondary text-primary"><Check size={17} /></div><div><h3 className="font-display text-xl text-primary">{title}</h3><p className="mt-2 text-sm leading-5 text-muted-foreground">{body}</p></div></div>)}</section></div>;
}

export function AboutPage() {
  return <div><section className="bg-primary text-primary-foreground"><div className="mx-auto max-w-[1240px] px-5 py-20 sm:px-8 sm:py-28"><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">The Tanmatra method</p><h1 data-testid="heading-about" className="mt-5 max-w-4xl font-display text-5xl font-semibold leading-[.92] tracking-[-.04em] sm:text-8xl">Health food<br /><em className="font-normal text-accent">should still feel like food.</em></h1><p className="mt-8 max-w-xl text-base leading-7 text-primary-foreground/70 sm:text-lg">Tanmatra started with a simple frustration: nutrition advice was everywhere, but a good Tuesday lunch was still hard to find.</p></div></section><section className="mx-auto max-w-[1240px] px-5 py-20 sm:px-8 sm:py-28"><div className="grid gap-12 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">Our point of view</p><h2 className="mt-4 font-display text-4xl font-semibold leading-none text-primary sm:text-5xl">Clinical care, expressed through a warm plate.</h2></div><div className="space-y-5 text-base leading-8 text-muted-foreground"><p>We make meals for people who care about their health but do not want every meal to feel like homework. That means real spices, satisfying textures and portions that leave room for the rest of your day.</p><p>Our registered dietitian-led process brings precision to the background: macro splits, sodium limits and ingredient choices are considered before anything reaches the menu.</p></div></div><div className="mt-20 grid gap-5 md:grid-cols-3">{[['01', 'Dietitian designs', 'Recipes begin with a nutritional intention and ingredients we can stand behind.'], ['02', 'Kitchen prepares', 'Our Noida kitchen cooks fresh each day with whole spices and cold-pressed oils.'], ['03', 'You receive', 'A considered meal arrives at your door, ready to be part of your actual life.']].map(([number, title, body]) => <div key={number} className="rounded-2xl border border-border bg-card p-7"><span className="font-data text-xs text-accent">{number}</span><div className="mt-12 flex h-11 w-11 items-center justify-center rounded-full bg-secondary text-primary"><Utensils size={17} /></div><h3 className="mt-5 font-display text-2xl text-primary">{title}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{body}</p></div>)}</div></section><section className="bg-secondary"><div className="mx-auto max-w-[1240px] px-5 py-16 text-center sm:px-8 sm:py-20"><p className="font-display text-3xl leading-tight text-primary sm:text-5xl">“The right meal is a small act of care<br className="hidden sm:block" /> you can repeat every day.”</p><p className="mt-6 text-xs font-bold uppercase tracking-[.16em] text-accent">Tanmatra kitchen notes</p><Link href="/menu" data-testid="link-about-menu" className="mt-9 inline-flex items-center gap-2 rounded-full bg-primary px-6 py-3 text-sm font-bold text-primary-foreground">Meet the menu <ArrowRight size={16} /></Link></div></section></div>;
}

export function CartPage() {
  const { cart, cartTotal, cartCount, changeQuantity, removeFromCart, showNotice } = useStorefront();
  const delivery = cartTotal >= 499 ? 0 : cartTotal > 0 ? 49 : 0;
  const total = cartTotal + delivery;
  return <div className="mx-auto max-w-[1080px] px-5 py-12 sm:px-8 sm:py-16"><div className="flex items-end justify-between gap-4"><div><p className="text-[11px] font-bold uppercase tracking-[.2em] text-accent">Your bag</p><h1 data-testid="heading-cart" className="mt-3 font-display text-5xl font-semibold leading-none text-primary sm:text-6xl">Good choices.</h1></div><span data-testid="text-cart-count" className="font-data text-xs text-muted-foreground">{cartCount} {cartCount === 1 ? 'item' : 'items'}</span></div>{cart.length === 0 ? <div data-testid="empty-cart-state" className="mt-12 flex min-h-[400px] flex-col items-center justify-center rounded-2xl border border-dashed border-border bg-card px-5 text-center"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-secondary text-primary"><Utensils size={24} /></div><h2 className="mt-6 font-display text-3xl text-primary">Your bag is waiting.</h2><p className="mt-2 max-w-sm text-sm leading-6 text-muted-foreground">Fill it with something warm, balanced and worth looking forward to.</p><Link href="/menu" data-testid="link-empty-cart-menu" className="mt-7 rounded-full bg-primary px-6 py-3 text-sm font-bold text-primary-foreground">Browse the menu</Link></div> : <div className="mt-10 grid gap-10 lg:grid-cols-[1fr_360px] lg:items-start"><div className="divide-y divide-border rounded-2xl border border-border bg-card px-5 sm:px-7">{cart.map((line) => <div key={line.id} data-testid={`row-cart-${line.id}`} className="flex gap-4 py-5"><div className="flex h-20 w-20 shrink-0 items-center justify-center rounded-xl bg-secondary text-primary"><Utensils size={20} /></div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><p className="font-display text-xl font-semibold leading-tight text-primary">{line.name}</p><p className="mt-1 text-xs text-muted-foreground">{line.kind === 'plan' ? 'Monthly plan' : 'Single serving'}</p></div><button type="button" aria-label={`Remove ${line.name}`} data-testid={`button-remove-${line.id}`} onClick={() => removeFromCart(line.id)} className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-muted-foreground hover:bg-secondary hover:text-destructive"><Trash2 size={15} /></button></div><div className="mt-4 flex items-center justify-between"><span className="font-data text-sm font-bold text-primary">{formatPrice(line.price * line.quantity)}</span><div className="flex items-center gap-1 rounded-full border border-border p-1"><button type="button" aria-label={`Decrease ${line.name}`} data-testid={`button-decrease-${line.id}`} onClick={() => changeQuantity(line.id, -1)} className="flex h-7 w-7 items-center justify-center rounded-full text-primary hover:bg-secondary"><Minus size={13} /></button><span data-testid={`text-quantity-${line.id}`} className="w-6 text-center font-data text-xs">{line.quantity}</span><button type="button" aria-label={`Increase ${line.name}`} data-testid={`button-increase-${line.id}`} onClick={() => changeQuantity(line.id, 1)} className="flex h-7 w-7 items-center justify-center rounded-full bg-primary text-primary-foreground hover:bg-accent hover:text-accent-foreground"><Plus size={13} /></button></div></div></div></div>)}</div><aside className="rounded-2xl bg-primary p-7 text-primary-foreground lg:sticky lg:top-28"><p className="text-[11px] font-bold uppercase tracking-[.18em] text-accent">Order summary</p><div className="mt-7 space-y-4 border-b border-primary-foreground/15 pb-5 text-sm"><div className="flex justify-between text-primary-foreground/70"><span>Subtotal</span><span className="font-data">{formatPrice(cartTotal)}</span></div><div className="flex justify-between text-primary-foreground/70"><span>Delivery</span><span className="font-data">{delivery === 0 ? 'Free' : formatPrice(delivery)}</span></div></div><div className="flex justify-between pt-5"><span className="font-display text-2xl">Total</span><span data-testid="text-cart-total" className="font-data text-lg font-bold">{formatPrice(total)}</span></div><button type="button" data-testid="button-checkout" onClick={() => showNotice('Checkout is coming soon — your bag is saved')} className="mt-7 flex h-13 w-full items-center justify-center rounded-full bg-accent text-sm font-bold text-accent-foreground transition-transform hover:scale-[1.02]">Continue to checkout <ArrowRight size={16} className="ml-2" /></button><p className="mt-4 text-center text-[11px] text-primary-foreground/50">Free delivery on orders above ₹499</p></aside></div>}<Link href="/menu" data-testid="link-cart-continue-shopping" className="mt-10 inline-flex items-center gap-2 text-sm font-bold text-primary"><ChevronLeft size={16} /> Continue shopping</Link></div>;
}