# Baseline for scripts/lint-dark-forks.ts.
#
# EMPTY, and that is the point. The gate shipped with 522 frozen violations
# because migrating them needed a token-mapping decision per Tailwind hue.
# That decision was made (sky/cyan carried primary CTAs, so they became gold;
# emerald -> sage, rose -> danger, neutrals -> the ink/surface/line ramp), the
# migration landed, and the register is now clear: components/wellness/,
# components/rd/, components/primitives/, the styleguide and the rest all read
# semantic tokens.
#
# So there is no pre-approved backlog left. Any violation the gate reports is
# new debt and fails the build outright.
#
# Add an entry here ONLY for a pre-existing violation discovered later that
# genuinely cannot be fixed in the same change — never for new code. One
# `path/to/file.tsx:LINE:rule-id` per line, same shrink-only discipline as
# scripts/component-drift-baseline.txt: a stale entry (one whose line no
# longer violates) also fails the gate, so progress cannot silently rot.
