# Placeholder-photography baseline — occurrences present when the gate landed.
#
# Not an approval list. A debt register. See scripts/lint-placeholder-assets.ts.
#
# The three protocol cards were retired by routing the catalog's OWN dish
# photography into them (lib/planPhotos.ts) — the plan pools already knew which
# dishes they serve, so that debt needed no shoot at all. The four ambient
# background washes were retired by deletion: each rendered at 3-10% opacity,
# so each cost a 1920x1080 download to show a stranger's kitchen almost
# invisibly behind our own hygiene claims.
# The landing FlipCard's sample dish went the same way as the protocol cards:
# it now renders a real dish from the plan's own pool.
#
# THE REGISTER IS EMPTY. Every placeholder the gate was created to track has
# been retired, so the gate now reports "no placeholder photography anywhere"
# and ANY new entry is a regression rather than inherited debt. Keep it empty:
# the fix for a missing photo is a real asset or no photo, never a stand-in.
#
# The list may only SHRINK: replace a placeholder with a real asset, then lower
# the count here — or delete the line when the file reaches zero. A count that
# no longer matches the tree fails the gate in BOTH directions, so the register
# cannot drift out of date in either the forgiving or the strict direction.
#
# Sorted, "<repo-relative path> <count>" per line, so additive edits in
# different alphabetical positions merge without conflict.
