# Test-reach baseline — test files that ran in NO workflow when the gate landed.
#
# Not an approval list. A debt register. See scripts/lint-test-reach.ts.
# The list may only SHRINK: wire a test into a workflow step, then delete its
# line here (a stale entry fails the gate).
#
# Sorted, one repo-relative path per line, so additive edits in different
# alphabetical positions merge without conflict.
#
# 2026-08-18: drained from 50 entries to 1. The other 49 are now named in
# verify.yml (see the two "Baseline recovery" blocks). They were all executed
# against a real Postgres first; four were genuinely broken and were fixed
# rather than wired in green — a stale-catalog cache race in two suites, an
# assertion pinning a client-supplied meal count the server correctly
# overrides, and an ops-gated route that 500'd instead of denying.
#
# The single survivor is NOT unreached. dispatch.bulkhead.test.ts runs in
# bulkhead-ci.yml, which invokes it through the `ci:bulkhead` package script
# rather than naming the file — and this gate matches filenames in workflow
# command text, so it cannot see through that indirection. It is listed here to
# keep the gate honest about its own blind spot rather than to record debt.
# Wiring it a second time in verify.yml would run a p95-latency load test twice
# per PR for no added signal.
artifacts/api-server/src/lib/dispatch.bulkhead.test.ts
