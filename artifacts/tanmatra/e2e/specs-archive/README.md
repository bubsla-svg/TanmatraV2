# Archived specs

Written against the pre-rework (System-A) app at base 2507084 — selectors and
the 5% GST fixture no longer match the shipped v2 app (GST is 18%, routes and
class names changed wholesale). Kept for reference while scenarios are ported
into specs/ against the real DOM. Not picked up by the runner (testDir=./specs).
