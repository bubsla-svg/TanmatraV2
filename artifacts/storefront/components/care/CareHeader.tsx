/** D-05B: the page's one h1. No subtitle copy — the ruling supplies no
 *  string for one, and the cards below declare the actual actions. */
export function CareHeader() {
  return <h1 className="text-3xl font-semibold tracking-tight text-ink">Care</h1>;
}
